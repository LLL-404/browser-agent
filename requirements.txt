# 核心依赖（与 pyproject.toml [project.dependencies] 保持一致）
camoufox>=0.3.0          # 浏览器自动化引擎（内置 C++ 级反检测）
mcp>=1.0.0
pyyaml>=6.0
aiofiles>=23.0
httpx>=0.27.0

# 推荐安装方式：
#   pip install -e ".[dev]"    # 含开发依赖（pytest/ruff/mypy 等）
#   pip install -e .          # 仅核心依赖
